# Commits that changed formatting or names but no behaviour. `git blame` skips these so
# it keeps pointing at the commit that actually wrote a line.
#
# Enable locally (GitHub honours this file automatically):
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it is provably mechanical — a wholesale reformat, or a pure
# rename. Never a commit that changed behaviour, or blame will lie about it.

# (commits are appended below as they land)
